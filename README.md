# Dimensionality-Reduction-Comparison
A facial recognition study utilizing Principal Component Analysis (PCA) for dimensionality reduction on the LFW (Labeled Faces in the Wild) dataset. The project evaluates and compares the performance of Support Vector Machines (SVM) and Logistic Regression when trained on reduced feature sets.
